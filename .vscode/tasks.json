{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C: tcc.exe build active file",
            "command": "${workspaceFolder}\\crossplatform_app\\tools\\tcc\\win32\\tcc.exe",
            "args": [
                "script_build.c",
                "-run"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}